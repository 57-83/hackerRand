def take(array, n=1)
    # So, in the implementation of the take method, array.drop(n) is used to drop the first n elements from the original array and return the rest of the elements as a new array.
   array.drop(n)
  end
puts take([1, 2, 3, 4, 5],3)
# So, in the implementation of the take method, array.drop(n) is used to drop the first n elements from the original array and return the rest of the elements as a new array.

